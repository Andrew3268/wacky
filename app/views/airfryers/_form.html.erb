<%= form_with(model: airfryer) do |form| %>
  <% if airfryer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(airfryer.errors.count, "error") %> prohibited this airfryer from being saved:</h2>

      <ul>
        <% airfryer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :af_title, style: "display: block" %>
    <%= form.text_field :af_title %>
    <%= form.text_field :af_image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>


