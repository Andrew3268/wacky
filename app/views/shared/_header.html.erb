<header class="header responsive-wrapper">
  <div class="header-left">
    <h1><a href="/">WACKY-THINK</a></h1>
  </div>
  <div class="header-middle">
    <% if current_user %> <!-- 로그인 중인 경우 -->
      <span>You are a <%= current_user.role %></span>
      <%= link_to "로그아웃", destroy_user_session_path, 'data-turbo-method': :delete %>
    <% end %>
    <span>/</span>
    <% if current_user && current_user.admin? %>
      <%= link_to "New airfryer", new_airfryer_path %>
      <%= link_to "로그아웃", destroy_user_session_path, 'data-turbo-method': :delete %>
    <% else %> <!-- 로그인하지 않은 경우 -->
      <%= link_to "가입하기", new_user_registration_path %>
      <%= link_to "로그인", new_user_session_path %>
    <% end %>
  </div>
  <div class="header-right">
    <nav class="header-nav">
      <a href="#" class="header-link header-link--button">Get 1 year for $50 USD</a>
    </nav>
  </div>
</header>